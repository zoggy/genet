- type to represent a rdf-schema
- function to build a rdf graph from such a schema structure
- add to schema: genet_file_ext, genet_desc

- read database kind in configuration file (mysql|postgresql)
  use this information when opening the storage

- configure: detect ocamlnet libraries

Add information:
- date of versions
- description of interfaces
- instanciated chains
- a class and resources for /tools/xxx/interfaces/yyy/input
- a class and resources for /tools/xxx/interfaces/yyy/output

Web interface:
- version: show branch tree above the version
  in a b1 / b2 / ... way

Ids:
In ids: forbid '-' (as in filetype ids): else can't match between ids in
.gnt file and ids in uri